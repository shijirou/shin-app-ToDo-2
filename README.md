# shin-app-ToDo-2
初級編１：ToDoリストです.-2

## 目次
- [背景](#背景)
- [要件定義](#要件定義)
  - [機能一覧](#機能一覧)
  - [技術スタック](#技術スタック)
  - [対象外](#対象外)
- [画面イメージ](#画面イメージ)
- [ユースケース](#ユースケース)
- [非機能要件](#非機能要件)
- [進捗状況](#進捗状況)
- [ER図](docs/er-diagram.md)（別ページ）

## 進行中プロジェクト: Trello風タスクボード

### 背景
- ユーザーはスクールでAIのみを使ったアプリ開発を学習中。
- 目的はアプリの完成度よりも「AIと一緒に開発を進める経験に慣れること」。多くのアプリを数こなしたい。
- 要件定義や計画をきちんと考えるプロセスは大事にしたいが、トークン(コスト)は節約したい。
- 実装は一気に進めず、計画を立てて少しずつ段階的に進めたい。

### 要件定義
（確定版）

**アプリ名**: Trello風タスクボード

#### 機能一覧
関連: [ER図](docs/er-diagram.md)

1. <a id="func-1"></a>リストの追加
2. <a id="func-2"></a>リストの名前編集
3. <a id="func-3"></a>リストの削除
4. <a id="func-4"></a>カードの追加（リスト内）
5. <a id="func-5"></a>カードの内容編集
6. <a id="func-6"></a>カードの削除
7. <a id="func-7"></a>カードをドラッグ&ドロップで並び替え・別リストへ移動
8. <a id="func-8"></a>データはサーバーのデータベース（PostgreSQL）に保存し、ブラウザを閉じたり、ブラウザのデータを消去したりしても消えない

#### 技術スタック
アプリを作るために使う技術・道具の一覧です（学習動画の指示に合わせています）。
指示: バックエンドは Java + Spring Boot、フロントエンドは React（Next.js は対象外）、データベースは PostgreSQL。それ以外のツールは、これらに合わせて選定。

**1. フロントエンド**

| 項目 | 使用するもの |
|---|---|
| 言語 | TypeScript |
| フレームワーク | React |
| ビルドツール | Vite |
| ドラッグ&ドロップ | dnd-kit |
| スタイリング | CSS Modules |

**2. バックエンド**

| 項目 | 使用するもの |
|---|---|
| 言語・フレームワーク | Java + Spring Boot |
| API形式 | REST API |
| データベース接続 | Spring Data JPA |

**3. データベース**

| 項目 | 使用するもの |
|---|---|
| RDBMS | PostgreSQL |

**4. 開発ツール**

| 項目 | 使用するもの |
|---|---|
| バージョン管理 | Git + GitHub |
| パッケージ管理（フロントエンド） | npm |
| パッケージ管理・ビルド（バックエンド） | Maven |

**選定理由（指示で決まっていないもの）**
- TypeScript: React と組み合わせるときの標準的な言語。型があるので、Java と同じくミスに早く気づける
- Vite: React の開発環境を作る標準的なツール（Next.js を使わない場合の定番）
- dnd-kit: React 向けのドラッグ&ドロップ用ライブラリ（vuedraggable は Vue.js 用のため不採用）
- CSS Modules: 普通の CSS の書き方のまま使えるため、今のプロトタイプの style.css を活かしやすい
- Spring Data JPA: Spring Boot から PostgreSQL を読み書きする標準的な方法
- npm: React / Vite に最初から付いてくる標準のパッケージ管理ツール
- Maven: Spring Boot の解説や教材で最もよく使われるビルドツール

#### 対象外
今回は作らない機能です。

- ログイン機能
- 複数ボード
- ラベル・期限・添付ファイルなどの高度な機能
- Next.js の使用（フロントエンドは React + Vite で作る）

### 画面イメージ

**1. ボード全体のレイアウト**
```
┌─────────────────────────────────────────────────────────┐
│  Trello風タスクボード                                     │
├─────────────┬─────────────┬─────────────┬───────────────┤
│ ToDo        │ 進行中       │ 完了         │               │
│┌───────────┐│┌───────────┐│┌───────────┐│  ┌─────────┐  │
││ カードA   │││ カードC   │││ カードE   ││  │+ リストを │  │
│└───────────┘│└───────────┘│└───────────┘│  │  追加    │  │
│┌───────────┐│┌───────────┐│             │  └─────────┘  │
││ カードB   │││ カードD   │││             │               │
│└───────────┘│└───────────┘│             │               │
│ + カードを  │ + カードを  │ + カードを  │               │
│   追加      │   追加      │   追加      │               │
└─────────────┴─────────────┴─────────────┴───────────────┘
```

**2. カード追加モーダル**
```
┌───────────────────────────────┐
│  カードを追加            [×]  │
├───────────────────────────────┤
│ 追加先リスト                  │
│ [ ToDo ▼ ]                    │
│                                │
│ 内容                          │
│ [_____________________]       │
│                                │
│              [ 追加 ]         │
└───────────────────────────────┘
```

**3. カード編集モーダル**
```
┌───────────────────────────────┐
│  カードを編集            [×]  │
├───────────────────────────────┤
│ 内容                          │
│ [_____________________]       │
│                                │
│  [ 削除 ]         [ 保存 ]    │
└───────────────────────────────┘
```

### ユースケース
代表的な例を記載しています。

<a id="uc-1"></a>
**1. カードを追加する**
- 主アクター: ユーザー
- 基本フロー:
  1. 「カードを追加」から追加モーダルを開く
  2. 追加先リストを選択する
  3. カードの内容を入力する
  4. 「追加」ボタンを押す
  5. 選択したリストの末尾にカードが追加され、データベースに保存される
- 異常系: 内容が空文字の場合はエラーとなり、追加できない

<a id="uc-2"></a>
**2. カードをドラッグ&ドロップで移動する**
- 主アクター: ユーザー
- 基本フロー:
  1. 移動したいカードをドラッグする
  2. 同じリスト内の別の位置、または別のリストにドロップする
  3. カードの並び順・所属リストが更新され、データベースに即時保存される
- 異常系: リストの外側など、ドロップ不可能な場所に落とした場合は移動前の位置に戻る

<a id="uc-3"></a>
**3. リストを削除する（カード入り）**
- 主アクター: ユーザー
- 基本フロー:
  1. リストヘッダーの「…」メニューから「リストを削除」を選ぶ
  2. 確認ダイアログが表示される
  3. 「OK」を選ぶとリストと中のカードがすべて削除され、データベースに保存される
- 異常系: 確認ダイアログで「キャンセル」を選んだ場合、リストは削除されず何も変わらない

### 非機能要件
- **対応ブラウザ**: 最新版のChrome / Edge / Firefoxを想定
- **レスポンシブ対応**: PCブラウザ専用（スマホ・タブレット対応は対象外。ドラッグ&ドロップもマウス操作を前提とする）
- **想定規模（パフォーマンス目安）**: 小規模利用を想定（リストは数件程度、カードはリストごとに数十件程度）。大量データ向けの性能最適化は行わない
- **データ永続化**: データはサーバー上のPostgreSQLに保存する。ブラウザを閉じたり、ブラウザのデータ（キャッシュ・サイトデータ）を消去したりしても保存内容は残る
- **動作環境の前提**: 利用時はバックエンド（Spring Boot）とデータベース（PostgreSQL）を起動しておく必要がある。当面は自分のPC上（ローカル環境）で動かすことを想定し、インターネット上への公開は対象外とする

### 進捗状況
- 要件定義（機能一覧・画面イメージ・ユースケース・非機能要件）まで確定。あわせて目次と[ER図](docs/er-diagram.md)（別ページ）を追加。
- モック/プロトタイプ（index.html / style.css / app.js）を作成済み。リスト・カードのCRUD、ドラッグ&ドロップ、localStorage保存まで一通り動作する状態（画面の見本として利用。本実装ではPostgreSQL保存に切り替える）。
- 技術スタックを学習動画の指示に合わせて確定（React + Java/Spring Boot + PostgreSQL）。データ保存先をlocalStorageからPostgreSQLに変更し、関連する要件とER図を更新。
- 次のステップ: 本実装の計画（開発環境の準備 → バックエンド → フロントエンドの順など）を立てる。

### 次回の再開メモ（2026-09-26時点）
- **完了済み**: 要件定義書（README）、目次（見出しリンクに修正済み）、[ER図](docs/er-diagram.md)（PostgreSQLのテーブル設計に更新）、技術スタック（学習動画の指示に合わせて確定）、モック/プロトタイプ3ファイル（localStorage版。画面の見本）。
- **次回の最初にやること**: 本実装の進め方の計画づくり（開発環境の準備、フォルダ構成、作る順番）。
- **未確認**: GitHub上でのER図（Mermaid）の描画。プロトタイプにあるカードの優先順位機能を要件に入れるかどうか。
- **進め方の方針**: 少しずつ段階的に進める。各工程の後に動作確認し、トークンは節約する。計画を立ててから本実装に進む。

