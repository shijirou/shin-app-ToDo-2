# shin-app-ToDo-2
初級編１：ToDoリストです.-2

## 目次
- [背景](#背景)
- [要件定義](#要件定義)
  - [機能一覧](#機能一覧)
  - [技術スタック](#技術スタック)
  - [対象外](#対象外)
- [画面イメージ](#画面イメージ)
- [ユースケース](#ユースケース)
- [非機能要件](#非機能要件)
- [進捗状況](#進捗状況)
- [ER図](docs/er-diagram.md)（別ページ）

## 進行中プロジェクト: Trello風タスクボード

### 背景
- ユーザーはスクールでAIのみを使ったアプリ開発を学習中。
- 目的はアプリの完成度よりも「AIと一緒に開発を進める経験に慣れること」。多くのアプリを数こなしたい。
- 要件定義や計画をきちんと考えるプロセスは大事にしたいが、トークン(コスト)は節約したい。
- 実装は一気に進めず、計画を立てて少しずつ段階的に進めたい。

### 要件定義
（確定版）

**アプリ名**: Trello風タスクボード

#### 機能一覧
関連: [ER図](docs/er-diagram.md)

1. <a id="func-1"></a>リストの追加
2. <a id="func-2"></a>リストの名前編集
3. <a id="func-3"></a>リストの削除
4. <a id="func-4"></a>カードの追加（リスト内）
5. <a id="func-5"></a>カードの内容編集
6. <a id="func-6"></a>カードの削除
7. <a id="func-7"></a>カードをドラッグ&ドロップで並び替え・別リストへ移動
8. <a id="func-8"></a>データはlocalStorageに保存し、再読み込みしても消えない

#### 技術スタック
アプリを作るために使う技術・道具の一覧です。ライブラリやフレームワークは使わず、ブラウザだけで動く最小構成にしています。

**アプリ本体**

| 分類 | 使う技術 | 役割（ひとことで） |
|---|---|---|
| 画面の骨組み | HTML | ボード・リスト・カード・モーダルなど、画面の部品を並べる |
| 見た目 | CSS | 色・配置・余白など、画面のデザインを整える |
| 動き | JavaScript（素のJS） | 追加・編集・削除・ドラッグ&ドロップなどの操作を動かす |
| ドラッグ&ドロップ | HTML Drag and Drop API | ブラウザに標準で入っている、つかんで動かす機能 |
| データの保存 | localStorage（JSON形式） | ブラウザ内にデータを保存し、再読み込みしても消えないようにする |

**ファイル構成**

| ファイル | 中身 |
|---|---|
| index.html | 画面の骨組み |
| style.css | 見た目 |
| app.js | 動きとデータの保存 |

**開発に使う道具**

| 道具 | 用途 |
|---|---|
| Cursor | コードを書く・README を見るエディタ |
| Chrome / Edge / Firefox | 作ったアプリを開いて動作を確認する |
| Git / GitHub | 変更の記録（履歴）と保存 |

**使わないもの（今回は不要）**
- サーバー・データベース（データはブラウザ内だけに保存するため）
- フレームワーク（React など）やライブラリ（学習用のため、素の構成にする）
- ビルドツール・インストール作業（`index.html` をブラウザで開くだけで動く）

#### 対象外
今回は作らない機能です。

- ログイン機能
- 複数ボード
- ラベル・期限・添付ファイルなどの高度な機能

### 画面イメージ

**1. ボード全体のレイアウト**
```
┌─────────────────────────────────────────────────────────┐
│  Trello風タスクボード                                     │
├─────────────┬─────────────┬─────────────┬───────────────┤
│ ToDo        │ 進行中       │ 完了         │               │
│┌───────────┐│┌───────────┐│┌───────────┐│  ┌─────────┐  │
││ カードA   │││ カードC   │││ カードE   ││  │+ リストを │  │
│└───────────┘│└───────────┘│└───────────┘│  │  追加    │  │
│┌───────────┐│┌───────────┐│             │  └─────────┘  │
││ カードB   │││ カードD   │││             │               │
│└───────────┘│└───────────┘│             │               │
│ + カードを  │ + カードを  │ + カードを  │               │
│   追加      │   追加      │   追加      │               │
└─────────────┴─────────────┴─────────────┴───────────────┘
```

**2. カード追加モーダル**
```
┌───────────────────────────────┐
│  カードを追加            [×]  │
├───────────────────────────────┤
│ 追加先リスト                  │
│ [ ToDo ▼ ]                    │
│                                │
│ 内容                          │
│ [_____________________]       │
│                                │
│              [ 追加 ]         │
└───────────────────────────────┘
```

**3. カード編集モーダル**
```
┌───────────────────────────────┐
│  カードを編集            [×]  │
├───────────────────────────────┤
│ 内容                          │
│ [_____________________]       │
│                                │
│  [ 削除 ]         [ 保存 ]    │
└───────────────────────────────┘
```

### ユースケース
代表的な例を記載しています。

<a id="uc-1"></a>
**1. カードを追加する**
- 主アクター: ユーザー
- 基本フロー:
  1. 「カードを追加」から追加モーダルを開く
  2. 追加先リストを選択する
  3. カードの内容を入力する
  4. 「追加」ボタンを押す
  5. 選択したリストの末尾にカードが追加され、localStorageに保存される
- 異常系: 内容が空文字の場合はエラーとなり、追加できない

<a id="uc-2"></a>
**2. カードをドラッグ&ドロップで移動する**
- 主アクター: ユーザー
- 基本フロー:
  1. 移動したいカードをドラッグする
  2. 同じリスト内の別の位置、または別のリストにドロップする
  3. カードの並び順・所属リストが更新され、localStorageに即時保存される
- 異常系: リストの外側など、ドロップ不可能な場所に落とした場合は移動前の位置に戻る

<a id="uc-3"></a>
**3. リストを削除する（カード入り）**
- 主アクター: ユーザー
- 基本フロー:
  1. リストヘッダーの「…」メニューから「リストを削除」を選ぶ
  2. 確認ダイアログが表示される
  3. 「OK」を選ぶとリストと中のカードがすべて削除され、localStorageに保存される
- 異常系: 確認ダイアログで「キャンセル」を選んだ場合、リストは削除されず何も変わらない

### 非機能要件
- **対応ブラウザ**: 最新版のChrome / Edge / Firefoxを想定
- **レスポンシブ対応**: PCブラウザ専用（スマホ・タブレット対応は対象外。ドラッグ&ドロップもマウス操作を前提とする）
- **想定規模（パフォーマンス目安）**: 小規模利用を想定（リストは数件程度、カードはリストごとに数十件程度）。大量データ向けの性能最適化は行わない
- **データ永続化の制約**: 同一ブラウザ・同一端末のlocalStorageにのみ保存する。他端末・他ブラウザとは同期しない。ブラウザのデータ（キャッシュ・サイトデータ）を消去すると保存内容も失われる

### 進捗状況
- 要件定義（機能一覧・画面イメージ・ユースケース・非機能要件）まで確定。あわせて目次と[ER図](docs/er-diagram.md)（別ページ）を追加。
- モック/プロトタイプ（index.html / style.css / app.js）を作成済み。リスト・カードのCRUD、ドラッグ&ドロップ、localStorage保存まで一通り動作する状態。
- 次のステップ: プロトタイプを実際に触って確認し、必要な調整・作り込み（本実装）を進める。

### 次回の再開メモ（2026-09-22時点）
- **完了済み**: 要件定義書（README）、目次（内部リンク）、[ER図](docs/er-diagram.md)、モック/プロトタイプ3ファイル（index.html / style.css / app.js）。プロトタイプはブラウザで動作確認済み、全体的に良い評価。
- **次回の最初にやること**: プロトタイプの見た目・操作感の細かい調整（ユーザーからの具体的な指摘待ち）。
- **未確認**: GitHub上での目次・ER図（Mermaid）のリンクと描画。
- **進め方の方針**: 少しずつ段階的に進める。各工程の後に動作確認し、トークンは節約する。プロトタイプの調整が終わったら本実装（またはプロトタイプの作り込み）に進む。

