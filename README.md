# shin-app-ToDo-2
初級編１：ToDoリストです.-2

## 目次
- [背景](#sec-background)
- [要件定義（確定）](#sec-requirements)
  - [機能一覧](#sec-features)
  - [技術構成](#sec-tech)
  - [対象外](#sec-out-of-scope)
- [画面イメージ](#sec-screens)
- [ユースケース（代表例）](#sec-usecases)
- [非機能要件](#sec-nfr)
- [進捗状況](#sec-progress)
- [ER図](docs/er-diagram.md)（別ページ）

## 進行中プロジェクト: Trello風タスクボード

<a id="sec-background"></a>
### 背景
- ユーザーはスクールでAIのみを使ったアプリ開発を学習中。
- 目的はアプリの完成度よりも「AIと一緒に開発を進める経験に慣れること」。多くのアプリを数こなしたい。
- 要件定義や計画をきちんと考えるプロセスは大事にしたいが、トークン(コスト)は節約したい。
- 実装は一気に進めず、計画を立てて少しずつ段階的に進めたい。

<a id="sec-requirements"></a>
### 要件定義（確定）
**アプリ名**: Trello風タスクボード

<a id="sec-features"></a>
**機能一覧**（関連: [ER図](docs/er-diagram.md)）
1. <a id="func-1"></a>リストの追加
2. <a id="func-2"></a>リストの名前編集
3. <a id="func-3"></a>リストの削除
4. <a id="func-4"></a>カードの追加（リスト内）
5. <a id="func-5"></a>カードの内容編集
6. <a id="func-6"></a>カードの削除
7. <a id="func-7"></a>カードをドラッグ&ドロップで並び替え・別リストへ移動
8. <a id="func-8"></a>データはlocalStorageに保存し、再読み込みしても消えない

<a id="sec-tech"></a>
**技術構成**
- HTML / CSS / JavaScript（素の構成、フレームワークなし）
- ファイルは index.html / style.css / app.js の3つ
- データ永続化: ブラウザのlocalStorageのみ（サーバー・DBなし）

<a id="sec-out-of-scope"></a>
**対象外（今回作らない）**
- ログイン機能
- 複数ボード
- ラベル・期限・添付ファイルなどの高度な機能

<a id="sec-screens"></a>
### 画面イメージ

**1. ボード全体のレイアウト**
```
┌─────────────────────────────────────────────────────────┐
│  Trello風タスクボード                                     │
├─────────────┬─────────────┬─────────────┬───────────────┤
│ ToDo        │ 進行中       │ 完了         │               │
│┌───────────┐│┌───────────┐│┌───────────┐│  ┌─────────┐  │
││ カードA   │││ カードC   │││ カードE   ││  │+ リストを │  │
│└───────────┘│└───────────┘│└───────────┘│  │  追加    │  │
│┌───────────┐│┌───────────┐│             │  └─────────┘  │
││ カードB   │││ カードD   │││             │               │
│└───────────┘│└───────────┘│             │               │
│ + カードを  │ + カードを  │ + カードを  │               │
│   追加      │   追加      │   追加      │               │
└─────────────┴─────────────┴─────────────┴───────────────┘
```

**2. カード追加モーダル**
```
┌───────────────────────────────┐
│  カードを追加            [×]  │
├───────────────────────────────┤
│ 追加先リスト                  │
│ [ ToDo ▼ ]                    │
│                                │
│ 内容                          │
│ [_____________________]       │
│                                │
│              [ 追加 ]         │
└───────────────────────────────┘
```

**3. カード編集モーダル**
```
┌───────────────────────────────┐
│  カードを編集            [×]  │
├───────────────────────────────┤
│ 内容                          │
│ [_____________________]       │
│                                │
│  [ 削除 ]         [ 保存 ]    │
└───────────────────────────────┘
```

<a id="sec-usecases"></a>
### ユースケース（代表例）

<a id="uc-1"></a>
**1. カードを追加する**
- 主アクター: ユーザー
- 基本フロー:
  1. 「カードを追加」から追加モーダルを開く
  2. 追加先リストを選択する
  3. カードの内容を入力する
  4. 「追加」ボタンを押す
  5. 選択したリストの末尾にカードが追加され、localStorageに保存される
- 異常系: 内容が空文字の場合はエラーとなり、追加できない

<a id="uc-2"></a>
**2. カードをドラッグ&ドロップで移動する**
- 主アクター: ユーザー
- 基本フロー:
  1. 移動したいカードをドラッグする
  2. 同じリスト内の別の位置、または別のリストにドロップする
  3. カードの並び順・所属リストが更新され、localStorageに即時保存される
- 異常系: リストの外側など、ドロップ不可能な場所に落とした場合は移動前の位置に戻る

<a id="uc-3"></a>
**3. リストを削除する（カード入り）**
- 主アクター: ユーザー
- 基本フロー:
  1. リストヘッダーの「…」メニューから「リストを削除」を選ぶ
  2. 確認ダイアログが表示される
  3. 「OK」を選ぶとリストと中のカードがすべて削除され、localStorageに保存される
- 異常系: 確認ダイアログで「キャンセル」を選んだ場合、リストは削除されず何も変わらない

<a id="sec-nfr"></a>
### 非機能要件
- **対応ブラウザ**: 最新版のChrome / Edge / Firefoxを想定
- **レスポンシブ対応**: PCブラウザ専用（スマホ・タブレット対応は対象外。ドラッグ&ドロップもマウス操作を前提とする）
- **想定規模（パフォーマンス目安）**: 小規模利用を想定（リストは数件程度、カードはリストごとに数十件程度）。大量データ向けの性能最適化は行わない
- **データ永続化の制約**: 同一ブラウザ・同一端末のlocalStorageにのみ保存する。他端末・他ブラウザとは同期しない。ブラウザのデータ（キャッシュ・サイトデータ）を消去すると保存内容も失われる

<a id="sec-progress"></a>
### 進捗状況
- 要件定義（機能一覧・画面イメージ・ユースケース・非機能要件）まで確定。あわせて目次と[ER図](docs/er-diagram.md)（別ページ）を追加。実装ファイルはまだ何も作成していない（index.html / style.css / app.js は未作成）。
- 次のステップ: 実装を工程ごとに分割した計画を立て、少しずつ実装を進める。

### 次回の再開メモ（2026-09-19時点）
- **完了済み**: 要件定義書（README）、目次（内部リンク）、[ER図](docs/er-diagram.md)（要件へのリンク付き）。
- **未着手**: アプリ本体（index.html / style.css / app.js）。ユーザーの指示があるまで作り始めない。
- **次回の最初にやること**: 実装計画を立てる。下記のたたき台を確認・調整してから、1工程ずつ実装する。
  1. 骨組み（3ファイル作成、ヘッダーとボード領域、「+ リストを追加」ボタンの表示）
  2. データ層（lists / cards の state、localStorageの load / save、初期データ）
  3. リスト機能（追加・名前編集・削除。削除は確認ダイアログ付き）
  4. カード機能（追加・編集モーダル、内容が空ならエラー、削除）
  5. ドラッグ&ドロップ（並び替え・別リスト移動、即時保存）
  6. 仕上げ（再読み込みでの保持確認、見た目の調整、READMEの進捗更新）
- **未確認**: GitHub上での目次・ER図（Mermaid）のリンクと描画。次回、最初に確認する。
- **進め方の方針**: 少しずつ段階的に進める。各工程の後に動作確認し、トークンは節約する。

